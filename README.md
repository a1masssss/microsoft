# Butaq Team - Система анализа финансовых транзакций

## 🏆 Достижение

**Мы заняли 2 место!** 🥈

---

## 📋 О проекте

Это комплексная система для анализа финансовых транзакций с использованием искусственного интеллекта. Проект позволяет пользователям задавать вопросы на естественном языке и получать аналитические данные из базы транзакций, содержащей более 11.5 миллионов записей.

### Основные возможности

- 🤖 **AI-агент для SQL-запросов**: Преобразование естественного языка в SQL-запросы с использованием LangChain и OpenAI
- 💬 **Telegram Mini App**: Интеграция с Telegram для удобного доступа через мессенджер
- 📊 **Визуализация данных**: Интерактивные графики и диаграммы с использованием Plotly
- 🔍 **Глубокий анализ**: Расширенные инструменты для детального анализа транзакций
- 📈 **MCP Protocol**: Интеграция с Model Context Protocol для работы с AI-моделями
- 🗄️ **Большие данные**: Эффективная работа с миллионами транзакций

---

## 🛠️ Технологический стек

### Backend

- **Django 5.2.8** - веб-фреймворк
- **PostgreSQL 15** - база данных
- **Django REST Framework** - REST API
- **LangChain** - фреймворк для работы с AI
- **OpenAI API** - для генерации SQL-запросов
- **Google Gemini API** - альтернативный AI-провайдер
- **Plotly** - генерация визуализаций
- **Pandas & NumPy** - обработка данных
- **Django Unfold** - современный админ-интерфейс
- **WhiteNoise** - обслуживание статических файлов
- **Uvicorn** - ASGI-сервер
- **Docker & Docker Compose** - контейнеризация

### Frontend

- **React 18** - UI библиотека
- **TypeScript** - типизированный JavaScript
- **Vite** - сборщик и dev-сервер
- **Tailwind CSS** - стилизация
- **Plotly.js** - интерактивные графики
- **Framer Motion** - анимации
- **Radix UI** - компоненты интерфейса
- **Telegram Web App SDK** - интеграция с Telegram
- **Axios** - HTTP-клиент

### Инфраструктура

- **Docker** - контейнеризация приложений
- **Nginx** - веб-сервер и reverse proxy
- **PostgreSQL** - реляционная база данных
- **Certbot** - SSL-сертификаты

---

## 📁 Структура проекта

```
microsoft/
├── backend/              # Django backend
│   ├── backend/         # Основные настройки Django
│   ├── mcp/            # MCP Protocol и AI-агент
│   ├── telegram/       # Telegram интеграция
│   └── manage.py
├── frontend/            # React приложение для Telegram Mini App
│   ├── src/
│   │   ├── components/  # React компоненты
│   │   ├── pages/      # Страницы приложения
│   │   ├── api/        # API клиенты
│   │   └── hooks/      # React хуки
│   └── package.json
├── web/                 # Веб-приложение (публичный сайт)
│   ├── src/
│   └── package.json
├── docker-compose.yml   # Docker Compose конфигурация
├── requirements.txt     # Python зависимости
└── README.md
```

---

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Python 3.12+ (для локальной разработки)
- Node.js 18+ (для фронтенда)

### Установка и запуск

1. **Клонируйте репозиторий**
   ```bash
   git clone <repository-url>
   cd microsoft
   ```

2. **Создайте файл `.env`**
   ```env
   DB_NAME=your_db_name
   DB_USER=your_db_user
   DB_PASSWORD=your_db_password
   DB_HOST=db
   DB_PORT=5432
   SECRET_KEY=your_secret_key
   ALLOWED_HOSTS=localhost,127.0.0.1
   OPENAI_API_KEY=your_openai_key
   GEMINI_API_KEY=your_gemini_key
   VITE_API_BASE_URL=http://localhost:8000
   ```

3. **Запустите через Docker Compose**
   ```bash
   docker-compose up --build
   ```

4. **Примените миграции**
   ```bash
   docker-compose exec backend python manage.py migrate
   ```

5. **Загрузите тестовые данные (опционально)**
   ```bash
   docker-compose exec backend python manage.py load_sample_transactions
   ```

6. **Создайте суперпользователя**
   ```bash
   docker-compose exec backend python manage.py createsuperuser
   ```

Приложение будет доступно по адресам:
- Backend API: http://localhost:8000
- Admin панель: http://localhost:8000/admin
- API документация: http://localhost:8000/api/docs
- Frontend: http://localhost:80

---

## 🎯 Основные функции

### 1. AI-агент для SQL-запросов

Система использует LangChain SQL Agent для преобразования естественного языка в SQL-запросы:

- Автоматическая генерация SQL из текстовых запросов
- Валидация и проверка запросов перед выполнением
- Кэширование результатов для оптимизации производительности
- Поддержка сложных аналитических запросов

### 2. Telegram Mini App

Полнофункциональное приложение в Telegram:

- Чат-интерфейс для запросов
- История взаимодействий
- Визуализация результатов
- Аутентификация через Telegram

### 3. Визуализация данных

Автоматическая генерация графиков:

- Линейные графики
- Столбчатые диаграммы
- Круговые диаграммы
- Тепловые карты
- Временные ряды

### 4. MCP Protocol

Интеграция с Model Context Protocol:

- Поддержка JSON-RPC 2.0
- Регистрация инструментов
- Управление сессиями
- Логирование запросов и ответов

---

## 📊 Модели данных

### Transaction

Основная модель для хранения транзакций:

- `transaction_id` - уникальный идентификатор
- `transaction_timestamp` - время транзакции
- `card_id` - ID карты
- `merchant_id` - ID мерчанта
- `transaction_amount_kzt` - сумма в тенге
- `mcc_category` - категория мерчанта
- И другие поля...

### OpenAIMCPRequest/Response

Модели для отслеживания MCP-запросов и ответов.

### SQLToolExecution

Логирование выполнения SQL-инструментов LangChain.

---

## 🔧 API Endpoints

### Telegram API
- `POST /api/telegram/auth/` - аутентификация
- `POST /api/telegram/chat/` - отправка сообщений
- `GET /api/telegram/history/` - история чата

### MCP API
- `POST /api/mcp/tools/list/` - список инструментов
- `POST /api/mcp/tools/call/` - вызов инструмента
- `POST /api/mcp/query/` - выполнение запроса
- `GET /api/mcp/visualizations/` - получение визуализаций

### Документация
- `GET /api/docs/` - Swagger UI
- `GET /api/redoc/` - ReDoc
- `GET /api/schema/` - OpenAPI схема

---

## 🧪 Разработка

### Локальная разработка Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r ../requirements.txt
python manage.py runserver
```

### Локальная разработка Frontend

```bash
cd frontend
npm install
npm run dev
```

### Запуск тестов

```bash
docker-compose exec backend python manage.py test
```

---

## 📝 Миграции

```bash
# Создание миграций
docker-compose exec backend python manage.py makemigrations

# Применение миграций
docker-compose exec backend python manage.py migrate
```

---

## 🔐 Безопасность

- Использование переменных окружения для секретов
- CORS настройки для Telegram Mini App
- Валидация SQL-запросов перед выполнением
- Аутентификация через Telegram
- Защита от SQL-инъекций через LangChain

---

## 📈 Производительность

- Индексы на часто используемых полях
- Кэширование AI-агентов
- Оптимизация SQL-запросов
- Пагинация результатов
- Асинхронная обработка через Uvicorn

---

## 🤝 Команда

**Butaq Team**

---

## 📄 Лицензия

Проект разработан для хакатона/соревнования.

---

## 🙏 Благодарности

Спасибо организаторам за возможность участия и за 2 место! 🎉

